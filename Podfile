# Uncomment the next line to define a global platform for your project
platform :ios, '11.0'

target 'Cryptohopper-iOS-SDK' do
   use_frameworks!
end

target 'Cryptohopper-iOS-SDKTests' do
  use_frameworks!
  # Pods for testing
  pod 'Quick'
  pod 'Nimble'
end
